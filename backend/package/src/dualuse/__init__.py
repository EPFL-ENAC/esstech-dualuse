"""Example standalone package for the esstech-dualuse backend."""


def greet(name: str) -> str:
    """Return a friendly greeting for `name`.

    This is a placeholder function. Replace it with real logic for your project.
    """

    return f"Hello, {name}!"


def main() -> None:
    """Command-line entry point."""

    print(greet("world"))
